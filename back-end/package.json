{
  "name": "startup-clix",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "package": "sam package --template-file template.yaml --s3-bucket startup-clix --output-template-file packaged.yaml",
    "deploy": "sam deploy --template-file packaged.yaml --stack-name startup-clix --capabilities CAPABILITY_IAM"
  },
  "author": "Kay Plößer <k@kay.is> (http://kay.is)",
  "license": "GPL-3.0",
  "dependencies": {
    "pusher": "^1.5.1"
  }
}
